# Daily mobile backend (emulator + physical development builds).
EXPO_PUBLIC_API_URL=https://miayudatics-v1-0.onrender.com

# Loopback (127.0.0.1 / 10.0.2.2) is blocked unless this is exactly 1.
# That opt-in is not the daily workflow. Local :18080 is for server tests/smoke.
# EXPO_PUBLIC_ALLOW_LOCAL_API=1

# Smoke API (scripts/smoke-api.sh) — solo local/CI, no commitear valores reales
# SMOKE_FUNC_EMAIL=
# SMOKE_TEC_EMAIL=
# SMOKE_LIDER_EMAIL=
# SMOKE_PASSWORD=
