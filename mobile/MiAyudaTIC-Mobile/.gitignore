# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local
.expo-export-test/

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# local simulation / E2E uploads (not product assets)
# (do not add new file-* from adb/API probes)

# opencode.json is per-developer (contains user-specific paths and API keys)

# opencode.json is per-developer (contains user-specific paths and API keys)
# Protected at workspace root .gitignore (this one is a no-op for that file)
